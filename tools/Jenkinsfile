pipeline {
    agent any
    tools {
        maven 'apache-maven' 
    }
    stages {
        stage('Example') {
            steps {
                sh 'mvn --version'
            }
        }
    }
}
